/**
 * pet-tutor 模块配置：移植到其他项目时只需要改这里，
 * 也可以在使用 <PetTutor> 时用同名 props 覆盖。
 */
export const DEFAULT_API_BASE = 'http://127.0.0.1:8080'   // 8080 让位给 WisdomL 后端
export const DEFAULT_MODEL_URL = '/models/jj3.vrm'
export const DEFAULT_CHARACTER_ID = 'jenny'
export const DEFAULT_SESSION_ID = 'test-session-001'
